import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mycompany.insectscanner',
  appName: 'Thế Giới Côn Trùng Kỳ Diệu',
  webDir: 'dist'
};

export default config;
